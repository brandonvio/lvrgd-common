generate a docker compose file that will run all services in this project... and then... update the pull request github actions process... to start all the containers... and then run the integration tests on those containers... you will have to figure out how to set the environment variables the services are expecting... all environemnt variables the services need are in .env file. you'll ensure the settings in the docker compose file are surfaced to the integration tests... and then run the integration tests on those containers.